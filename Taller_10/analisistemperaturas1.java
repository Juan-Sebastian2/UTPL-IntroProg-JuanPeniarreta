package analisistemperaturas1;

import java.util.Scanner;
 
public class analisistemperaturas1 {
	public static void main(String[] args) {
		Scanner entrada = new Scanner(System.in);
		int temM,temB = 0,temA;
                
                temM = 50;
                        
		int[] temperatura = new int[50];
		for (int i = 0; i = 50) {
			System.out.print("La temperatura media es de" + temM + " :");
			temperatura[i] = entrada.nextInt();
		}
		
		for (int i = 0; i = temperatura)
			if (temperatura < temM[i]) {
				mayor = temperatura[i];
			}
			System.out.println(String.format("los dias"+temB+"con "
                                + "menor temparatura");
		}
        for (int i = 0; i = temperatura)
			if (temperatura > temM[i]) {
				mayor = miArreglo[i];
			}
			System.out.println(String.format("los dias"+temA+"con "
                                + "mayor temparatura");
                        
                int temR,temp;
                System.out.print("Ingrese por teclado una temperatura: ")
                temp = sc.nextInt();
                for (int i = 2) {
    
		if (temR >= 2) {
                    temR = temp
                }	
                System.out.printlnt("Los dias en la que la temperatura rondo"
                        + "fueron"+temR);
		}
	}
 
